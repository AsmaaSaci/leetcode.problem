public class Solution {
    public int SmallestRepunitDivByK(int k) {
        if((k&1) == 0 || k%5 == 0) return -1;
        int len = 1, rem = 1;
        while(rem%k != 0)
        {
            len++;
            rem = (rem*10+1)%k;
        }

        return rem%k == 0?len:-1;
    }
}
