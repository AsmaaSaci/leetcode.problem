public class Solution {
    public int MinOperations(int k) {
        int res = k;
        for (int i = 1; i * i <= k; i++) {
            int count = (k - 1) / i + 1;
            res = Math.Min(res, i - 1 + count - 1);
        }
        return res;
    }
}
