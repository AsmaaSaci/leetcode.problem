public string GetSmallestString(int n, int k)
{      
    var res = new char[n--];
    Array.Fill(res, 'a');
    
    while (n >= 0 && k > n)
    {
        int c = Math.Min(26, k - n);
        k -= c;
        res[n--] = (char)(c + 'a' - 1);
    }
    
    return new String(res);
}
