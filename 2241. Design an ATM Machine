public class ATM {
    private int[] freq;
    private int[] quant =  [20, 50, 100, 200, 500];
    public ATM() {
        freq = new int[5]; // [20, 50, 100, 200, 500]
    }
    
    public void Deposit(int[] banknotesCount) {
        for(int i = 0; i < 5; i++)
        {
            freq[i] += banknotesCount[i];
        }
    }
    
    public int[] Withdraw(int amount) {
        int[] res = new int[5];
        for(int i = 4; i >= 0; i--)
        {
            if(freq[i] == 0)
                continue;

            if(amount >= quant[i])
            {
                int cnt = amount/quant[i];
                cnt = Math.Min(cnt, freq[i]);
                res[i] = cnt;
                amount -= cnt*quant[i];
            }
        }

        if(amount > 0)
            return [-1];

        for(int i = 0; i < 5; i++)
        {
            freq[i]-=res[i];
        }

        return res;
    }
}

/**
 * Your ATM object will be instantiated and called as such:
 * ATM obj = new ATM();
 * obj.Deposit(banknotesCount);
 * int[] param_2 = obj.Withdraw(amount);
 */
