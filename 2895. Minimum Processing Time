public class Solution
{
    public int MinProcessingTime(IList<int> processorTime, IList<int> tasks)
    {
        return tasks.OrderByDescending(x => x).Where((_, i) => i % 4 == 0).Zip(processorTime.OrderBy(x => x)).Select(x => x.First + x.Second).Max();
    }
}
