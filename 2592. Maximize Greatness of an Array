public class Solution {
    public int MaximizeGreatness(int[] nums)
    {
        Array.Sort(nums);
        int res = 0;
        for (int i = 1; i < nums.Length; i++)
            if (nums[i] > nums[res])
                res++;
        return res;
    }
}
``'
