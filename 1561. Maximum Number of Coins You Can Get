public class Solution {
    public int MaxCoins(int[] piles) {
        Array.Sort(piles);
        int ans = 0;
        for (int i =0, j= piles.Length -2; i < j; i++, j-=2)
        {
            ans += piles[j];
        }
        return ans;    
    }
}
